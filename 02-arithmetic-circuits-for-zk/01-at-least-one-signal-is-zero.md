# At least one signal is zero

## Problem

Create an arithmetic circuit that takes signals $x₁, x₂, …, x_n$ and is satisfied if at least one signal is $0$.

## Solution

$0 \equiv x₁ \cdot x_2 \cdot ... \cdot x_n$
